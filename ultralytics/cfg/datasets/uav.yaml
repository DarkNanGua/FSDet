# Ultralytics YOLO 🚀, AGPL-3.0 license
# COCO128 dataset https://www.kaggle.com/ultralytics/coco128 (first 128 images from COCO train2017) by Ultralytics
# Example usage: yolo train data=coco128.yaml
# parent
# ├── ultralytics
# └── datasets
#     └── coco128  ← downloads here (7 MB)


# Train/val/test sets as 1) dir: path/to/imgs, 2) file: path/to/imgs.txt, or 3) list: [path/to/imgs1, path/to/imgs2, ..]
path: /home/ng2/Deng/datasets/uav_yao/datasets  # dataset root dir
train: train/images  # train images (relative to 'path') 128 images
val: val/images  # val images (relative to 'path') 128 images
test: test/images # test images (optional)

# Classes
names:
  0: uav

#yolo detect train data=datasets/uav.yaml model=yolov11s.yaml pretrained=ultralytics/yolov11s.pt epochs=150 batch=8 lr0=0.01 resume=True
  #  yolo detect train data=dataset/uav.yaml model=yolov8n.yaml pretrained=ultralytics/yolov8n.pt epochs=150 batch=8 lr0=0.01 resume=True
#yolo detect train data=datasets/uav.yaml model=yolov8_GAM_SPDConv.yaml pretrained=ultralytics/yolov8s.pt epochs=150 batch=8 lr0=0.01 resume=True
